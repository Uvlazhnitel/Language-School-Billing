# -------------------------
# OS / editors / IDE
# -------------------------
.DS_Store
._*
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
.Spotlight-V100/
.Trashes/
.idea/                 # JetBrains/GoLand
.vscode/               # VS Code (if you want to share settings — remove this line)
*.swp
*.swo

# -------------------------
# Go / tests / profiles
# -------------------------
# Binaries and build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.obj
*.a
langschool              # Main application binary

# Profiles/cache/coverage
*.pprof
cpu.prof
mem.prof
heap.pprof
profile.out
coverage.out
cover.out
cover.html

# -------------------------
# Node / frontend (Wails, Vite)
# -------------------------
# Important: do NOT ignore lock files (package-lock.json / pnpm-lock.yaml / yarn.lock)!
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.eslintcache
frontend/.cache/
# Auto-generation may create intermediate directories:
dist/
.cache/

# -------------------------
# Wails
# -------------------------
build/                 # wails build outputs (bin/, *.app, etc.)
wails.log
# It is recommended NOT to ignore frontend/wailsjs — it is usually committed
# frontend/wailsjs/   # <— uncomment if you want to regenerate on CI

# -------------------------
# SQLite / data, if it ends up in the repository
# In reality, it is stored in the user's home directory, but just in case
# -------------------------
*.sqlite
*.sqlite3
*.db
*.db-shm
*.db-wal
*.sqlite-wal
*.sqlite-shm

# -------------------------
# Application data folders (if someone runs with a database in the repo directory)
# -------------------------
Data/
Backups/
Invoices/
Exports/
Fonts/                 # local TTF fonts (DejaVu) — store in the home directory; not needed in the repo

# If you store example configurations/fonts — create sample-assets/ directories and do not ignore them.

# -------------------------
# PDFs/documents generated by the application (if they end up in the repo)
# -------------------------
**/Invoices/**/*.pdf
*.pdf~                 # temporary

# -------------------------
# Logs
# -------------------------
*.log
logs/
log/

# -------------------------
# Temporary/auxiliary
# -------------------------
tmp/
temp/
*.tmp
*.bak
*.orig
*.rej

# -------------------------
# Env files (only .env.example should be in the repo)
# -------------------------
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example
!frontend/.env.example

# -------------------------
# Test snapshots/screenshots (if they appear)
# -------------------------
__snapshots__/
screenshots/
